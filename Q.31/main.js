//defined an empty array
var users_avaliable = [];
//checked wheather users are avaliable
if (users_avaliable.length === 0) {
    console.log("we need to find some users!");
}
else {
    console.log(users_avaliable);
}
